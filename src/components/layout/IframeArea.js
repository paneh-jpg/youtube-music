export const VideoArea = () => {
  return `
    <section class="rounded-xl bg-black/40 border border-white/10 overflow-hidden h-full">
      <div id="video-placeholder" class="relative h-full bg-black flex items-center justify-center">
        </div>
    </section>
  `;
};
